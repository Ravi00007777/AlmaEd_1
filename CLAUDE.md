# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

See `AGENTS.md` at the repo root — it holds the actual commands and architecture notes, written tool-agnostic so any AI coding agent can use it. Read that file first. Update it (not this one) when the codebase changes; this file exists only so Claude Code picks it up automatically.
