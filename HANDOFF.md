# Handoff

Current state of the project, for whoever picks this up next. Keep this updated as things move — it's the fastest way for a new contributor (human or AI) to know where things actually stand, without re-deriving it from the diff.

## Status: v1 verified end-to-end against a real Supabase project

Every core flow in `ARCHITECTURE.md` has actually been click-tested in a real browser against a real Supabase database — not just typechecked/built. Verified: student self-registration, admin-created teacher accounts, admin creates a batch (teacher + students + Meet link), admin schedules/cancels classes, teacher uploads a resource (confirmed the file is really in Supabase Storage and publicly downloadable), student sees their classes/resources, admin tracks payment status, and the class-reminder cron endpoint (confirmed it actually calls Resend without erroring, and stamps `reminderSentAt` so it won't double-send).

`npm run typecheck` and `npm run build` both pass clean (one benign upstream warning from `jose` about Edge Runtime compression APIs — cosmetic). `npm run lint` still hasn't been run to completion (kept hitting an interactive ESLint prompt that hung the session; `.eslintrc.json` exists now so it shouldn't prompt, just unconfirmed).

**Connected to a real Supabase project** (`azvmbeewzrgnexgxxcfl`). That project previously held a full, populated instance of the old deleted TutorConnect schema (32 tables, real row counts) — deliberately dropped (confirmed disposable test data) and replaced with the new schema via `prisma db push`. Admin account: `raviiit0007@gmail.com` / `12345678@` (changed from the `prisma/seed.ts` defaults — still no in-app way to change it, see below).

**Project location**: moved from `~/Documents/Default Project` (iCloud-synced, caused severe slowness — see `LESSONS.md`) to `~/Projects/edtech-platform` (local-only, not synced). Confirmed much faster after the move (route compiles went from 4–30+ seconds to ~1.2–1.5 seconds).

**UI/UX redesign done** (Tailwind config/CSS only, zero backend changes): indigo/teal/slate palette, Manrope font via `next/font/google`, a real sidebar+topbar dashboard shell (`src/components/dashboard-shell.tsx`) replacing the old flat header, and a small shared UI kit under `src/components/ui/` (Button, Card, Badge, Table, Avatar, EmptyState, PageHeader, hand-authored inline-SVG icons — no new npm packages, since this session's Bash tool was broken and couldn't run `npm install`). Every page's JSX was restyled to use these; every `prisma.*` query and every `actions.ts` file was left untouched — verified by clicking through as admin (overview, teachers, students, batches, batch detail, payments) plus a narrow-viewport check of the mobile nav drawer. Teacher and student dashboards use the identical shared components (already verified rendering correctly for admin) but weren't click-tested live under their own login in this session — no teacher/student password was on hand. Worth a quick pass next time those roles are logged into.

**QA test data in the DB**: created while verifying the redesign under real teacher/student logins (no existing teacher/student passwords were on hand) — teacher "QA Teacher" (`qa.teacher@example.com` / `QaTeacher123`), student "Test Student UI" (`test.student.ui@example.com`, no password — Google-only), a batch "QA Test Batch" pairing QA Teacher with Aarav Kumar plus one scheduled class, and a second batch "Notify Test Batch" (QA Teacher + the real student Ravi Gupta) used to verify the new batch-created/resource-shared emails below, including one uploaded test resource ("Notify Test Assignment", a placeholder PDF). Harmless but should be deleted via `db:studio` (or a future admin "remove" UI, see below) next time there's DB access — there's currently no in-app way to delete a teacher/student/batch/resource.

**Batch-created and resource-shared email notifications added** (`src/lib/notify.ts`, a thin `sendEmails()` wrapper around the existing `src/lib/resend.ts` client — mirrors the pattern already used by `/api/cron/class-reminders`): `createBatch` (`src/app/(dashboard)/admin/batches/actions.ts`) now emails the teacher and every initially-added student with the batch name, schedule note, and the persistent Meet link, right after the batch is created. `uploadResource` (`src/app/(dashboard)/teacher/batches/[id]/actions.ts`) now emails every student in the batch when a teacher shares a new assignment/test/note, including the deadline if one was set and a link to the file. Both are wrapped so a Resend failure is logged, never thrown — batch creation and resource upload must succeed regardless of email delivery. `RESEND_API_KEY`/`EMAIL_FROM` are already configured in `.env.local` (sender is `onboarding@resend.dev`, Resend's sandbox address — only reliably delivers to real, deliverable addresses, so the fake `@example.com` test accounts' emails won't actually land anywhere; the "Notify Test Batch" run above used a real Gmail address for that reason, but delivery wasn't independently confirmed from inside this session — worth a quick inbox check next time). Not yet covered: `addClass` (scheduling an individual class) doesn't send its own email — the existing 30-minutes-before cron reminder (`/api/cron/class-reminders`) already covers per-class notification, so this wasn't duplicated. Revisit if an immediate "class scheduled" email (not just the 30-min-before reminder) turns out to be wanted too.

**Known cleanup needed**: `src/app/api/dev/update-admin/route.ts` is a disabled stub (returns HTTP 410) — it was a temporary one-off route used to update the admin's email/password directly through the browser when the session's shell tool broke (see `LESSONS.md`). It's harmless as-is but should be deleted, along with the empty `api/dev/` directory, next time a shell is available.

Bugs found and fixed during real click-through testing (typecheck/build didn't catch any of these — worth remembering that a green build isn't proof a flow works):
- Login redirected to the public homepage `/` instead of the role's dashboard — `src/middleware.ts` only redirected logged-in users away from `/login`/`/register`, not `/`. Fixed by including `/` in that redirect set.
- After scheduling a class, uploading a resource, or recording a payment, the page didn't visually update until a manual reload — the data was correctly saved (`revalidatePath` worked), but a `useFormState`-driven form submission doesn't automatically refresh the page the way a plain form action does. Fixed by adding a `router.refresh()` in a `useEffect` on the returned state, in `AddClassForm`, `UploadResourceForm`, and `NewPaymentForm`.
- The shared `SubmitButton` component gave no visual feedback while a submission was in flight. On the (then-slow, pre-move) environment, a single "Create batch" click that took 4 seconds looked like nothing happened, so it got clicked repeatedly — creating 6 duplicate batches in the database (cleaned up manually). Fixed by having `SubmitButton` use `useFormStatus()` to disable itself and show "Please wait…" while pending — this fixes every form using the shared button at once, not just batch creation.

Payment status is now admin-only (was previously also shown to the student on their batch page) — the student-facing payment section was removed by request; see `ARCHITECTURE.md`'s updated "Core flows"/roles wording.

**Public student self-registration closed** (by request — admin wants to vet every account, same reasoning as teachers): `/register` no longer has a working form; it now shows a "contact your admin" message and a link back to `/login`. `registerStudent` in `src/app/(auth)/register/actions.ts` is neutered to always return an error (kept only so the Server Action ID isn't silently removed). Students are now created the same way teachers are — `src/app/(dashboard)/admin/students/actions.ts` (`createStudent`) and `/admin/students/new`, with an optional password (a student with none set can only log in via "Continue with Google" using the matching email). `src/lib/auth.ts`'s Google `signIn` callback no longer auto-creates a `STUDENT` on first Google sign-in from an unrecognized email — it now rejects any email that doesn't already belong to an existing student, exactly like it already did for teacher/admin emails.

**Google sign-in added** (students only, additive alongside email/password): `src/lib/auth.ts`'s `signIn` callback auto-registers a new Google account as a `STUDENT`, and rejects Google sign-in for any email that already belongs to a teacher/admin (keeps admin's teacher-vetting model intact). `User.passwordHash` is now nullable to support Google-only accounts; credentials login correctly rejects those instead of crashing. Verified end-to-end with a real Google account. The `GOOGLE_CLIENT_SECRET` in `.env.local` briefly touched a git-tracked file earlier in this project's history (never committed) — flagged to the project owner as worth rotating; not yet confirmed done.

Two CLI gotchas worth knowing if you hit them again: the Prisma CLI only auto-loads `.env`, not `.env.local` (Next.js loads both) — a root `.env` exists holding just `DATABASE_URL`/`DIRECT_URL`, duplicated from `.env.local`, keep them in sync manually. And `prisma db push` can throw a confusing `P1014` error when pushing a schema very different from what's currently in the DB in one shot — the fix that worked here was dropping the old tables/enum types explicitly first, then pushing onto a clean schema.

One more environment quirk hit repeatedly: this project's working directory appears to be on a slow-synced disk (iCloud Drive) — `rm -rf .next`, `next build`, and even individual dev-server route compiles have intermittently hung for 30s+ or longer with 0% CPU, resolved by killing and restarting the dev server. Not a code problem; just don't assume a hang is a bug before checking `ps` CPU usage on the `next-server`/`next dev` process.

## Done

- Prisma schema (`prisma/schema.prisma`): `User`, `Batch`, `BatchStudent`, `Class`, `Resource`, `Payment`, `Notification` — matches `ARCHITECTURE.md`'s data model sketch exactly.
- Auth: NextAuth v5, Credentials provider, JWT sessions, role-based middleware (`src/middleware.ts`), split into `auth.config.ts` (Edge-safe) + `auth.ts` (Node-only, Credentials provider).
- Admin: teacher creation, batch creation (with student picker), class scheduling/cancellation, payment tracking (create + toggle paid/pending) — all verified working live.
- Teacher: batch list with upcoming classes, resource upload to Supabase Storage — verified, including the uploaded file actually being fetchable.
- Student: batch list with upcoming classes and join links, resource list. No payment visibility (admin-only, by request).
- Cron: `/api/cron/class-reminders`, guarded by `CRON_SECRET`, wired into `vercel.json` — verified it runs, calls Resend, and stamps `reminderSentAt`.

**Doubt chat added** (student ↔ teacher, private per student, admin read-only, ~15s auto-refresh): new `DoubtMessage` model in `prisma/schema.prisma`. Same stale-client situation as `dueAt` — this session's dev server can't see `prisma.doubtMessage`, so `src/lib/doubts.ts` reads/writes it via raw SQL (`$queryRaw`/`$executeRaw`) instead, clearly marked TEMPORARY; delete that file's raw-SQL bodies and switch to typed `prisma.doubtMessage.*` calls once the server has restarted. The table itself already exists on the live (shared local+prod) DB — created once via `src/app/api/dev/create-doubt-table/route.ts`, now disabled to a `410` stub. Shared action: `src/lib/doubt-actions.ts` (`postDoubtMessage`) — used by both the student's "ask" form and the teacher's "reply" form, reuses the `resources` Supabase bucket under a `doubts/` prefix for image attachments. Shared UI: `src/components/doubt-chat.tsx`, `src/components/doubt-student-picker.tsx`. Wired into all three batch-detail pages (`admin/batches/[id]`, `teacher/batches/[id]`, `student/batches/[id]`). Verified live end-to-end: teacher → student reply, student → teacher reply, admin read-only view, and the mobile file-input styling.

**Contact-info filter on doubt chat** (`src/lib/contact-filter.ts`, used by `postDoubtMessage`): blocks message text containing an email address or a phone-number-shaped run of 7+ digits, per the platform's existing "teachers and students never exchange direct contact details" rule. Verified live — a message with a 10-digit number was correctly rejected with an inline error, a normal message with an unrelated number ("question 3") went through fine. This is a plain-text heuristic only — it cannot inspect image attachments for contact info.

**Admin "Remove" for teachers/students added**: soft delete via a new `User.isActive` column (default `true`) — a hard delete was confirmed earlier to be blocked by FKs (batches, resources, payments, accounts a user created) and would destroy real history anyway. "Remove" (red button, `src/app/(dashboard)/admin/teachers/page.tsx` and `admin/students/page.tsx`) sets `isActive = false`: the user can no longer log in (checked in both the Credentials `authorize()` and the Google `signIn` callback in `src/lib/auth.ts`), and disappears from the teachers/students lists and every selection dropdown (`admin/batches/new`, `admin/payments`) — but their existing batches/payments/resources/doubt threads keep rendering normally everywhere, untouched. Same stale-client situation as `dueAt`/`DoubtMessage`: `isActive` is bridged via `src/lib/user-status.ts` (raw SQL), used only at the specific call sites above — every other existing `prisma.user.*` call in the app is untouched. The DB column already exists on the live (shared local+prod) DB, added via the now-disabled `src/app/api/dev/add-user-isactive-column/route.ts`.

No reactivate UI yet (same "use `db:studio`" convention as other gaps) — flip `isActive` back to `true` directly in the DB if a removal needs undoing. **Known limitation, not fixed by this change**: sessions are JWT-based with no server-side session store, and `middleware.ts` runs on the Edge Runtime without Prisma access — so a removed user who already has an active browser session keeps working until it naturally expires or they sign out; "Remove" blocks *future* logins immediately but can't force an instant mid-session kick.

**Near-miss while testing this**: clicked "Remove" on a row, the list re-rendered, and a second click at the same screen coordinate landed on a different row — accidentally deactivated a real teacher account (`rbnoob876@gmail.com`) instead of just the intended test account. Caught immediately and reactivated via a one-off temporary route (created and disabled in the same turn). Worth remembering when testing any list where rows disappear after an action: re-screenshot before clicking again, don't reuse coordinates blind.

## Not started / known gaps

- **No password reset or in-app password change.** The seed script prints the admin password to the console once; admin-created teacher passwords are set by the admin and shared out of band. There is no flow to change a password after that. Fine for a first pass with a handful of users, not fine to leave forever.
- **No bulk/recurring class generation.** Admin schedules each `Class` occurrence one at a time via a datetime picker — `ARCHITECTURE.md`'s "Still open" section flagged this as an implementation detail to decide later; it hasn't been decided, it's just manual for now.
- **No admin UI to remove a single student from one batch, or delete a batch itself** — removing the teacher/student account entirely is now possible (see above), but un-enrolling one student from one batch while keeping their account, or deleting a batch outright, still has no UI. Do this directly in `db:studio` for now if needed.
- **Payment amounts have no currency field** — `Payment.amount` is a bare `Decimal`. Fine while there's one currency in play; will need a real decision if that changes.
- **No automated tests.**
- **`.env.local` still has the old dependencies' variables** (Google OAuth, Razorpay, Google Calendar, OpenAI, WhatsApp, Sentry, platform commission) left over from before the architecture reset — harmless (nothing reads them) but worth cleaning up once someone's touching that file anyway.
- **NextAuth logs a `MissingCSRF` error on every sign-out** even though sign-out completes correctly (verified repeatedly in browser testing) — cosmetic log noise from the beta version of next-auth v5, not an actual failure. Worth re-checking against a stable next-auth v5 release later, not worth chasing now.

## Open questions

- Whether a teacher can be on multiple batches at once — the schema supports it, never explicitly confirmed as a real use case.
- Recurring-schedule format for a batch (`scheduleNote` is currently free text, not a structured rule) — revisit if admin actually needs the platform to auto-generate future `Class` rows instead of adding them by hand.

## How to update this file

When a feature moves state (not started → in progress → done), edit the relevant line. When you resolve an open question, remove it and note the resolution in `LESSONS.md` if it's worth remembering why.
